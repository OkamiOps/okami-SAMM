# Porta do servidor (default 3000)
PORT=3000

# Caminho do banco SQLite (default ./data/okami-samm.db)
DB_PATH=./data/okami-samm.db

# IA opcional (sugestões do Roadmap). Sem chave, o botão de IA fica escondido.
ANTHROPIC_API_KEY=
# Modelo usado pelo proxy de IA
ANTHROPIC_MODEL=claude-sonnet-4-6
